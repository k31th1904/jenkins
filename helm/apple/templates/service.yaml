apiVersion: v1
kind: Service
metadata:
  name: {{ include "apple.fullname" . }}
  labels:
    {{- include "apple.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "apple.selectorLabels" . | nindent 4 }}
